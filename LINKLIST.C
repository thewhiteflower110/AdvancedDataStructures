/*      Author:- juhi kamdar
	Date of creation:-22nd jan 2018
	Email:-
	Objective:-to perform operations on link list :
			create the linklist
			insert the mode at the beginning
			insert the mode at the middle
			insert mode at the the end
			delete the mode at the beginning
			delete the mode at the middle
			delete mode at the the end
			display the linklist
			sort the linklist
				ascending
					swap the data
					swap the address
				descending
					swap the data
				       // swap the address


*/
#include<stdio.h>
#include<conio.h>
struct node{
	int info;
	struct node *next;

}*head=NULL;//the head is the global name of the first mode of list

void create();
void display();

printf()
void main()
	{

	clrscr();
	getch();
	}

